export class User {
	constructor(cinemaId, email, password, firstName, lastName, phoneNumber) {
		this.cinemaId = cinemaId;
		this.email = email;
		this.password = password;
		this.firstName = firstName;
		this.lastName = lastName;
		this.phoneNumber = phoneNumber;
	}
}