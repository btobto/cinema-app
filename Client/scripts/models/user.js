export class User {
	constructor(id, cinemaId, email, firstName, lastName, phoneNumber) {
		this.id = id;
		this.cinemaId = cinemaId;
		this.email = email;
		this.firstName = firstName;
		this.lastName = lastName;
		this.phoneNumber = phoneNumber;
	}
}