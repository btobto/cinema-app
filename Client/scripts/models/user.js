export class User {
	constructor(cinemaId, email, firstName, lastName, phoneNumber) {
		this.cinemaId = cinemaId;
		this.email = email;
		this.firstName = firstName;
		this.lastName = lastName;
		this.phoneNumber = phoneNumber;
	}
}